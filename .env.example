# Copy to .env and fill in real values. Never commit .env (see .gitignore).
# Read by ingestion/config.py; also consumed directly by docker-compose.yml
# for the local Postgres container.

PGHOST=localhost
PGPORT=5432
PGDATABASE=sentiment
PGUSER=postgres
PGPASSWORD=changeme
